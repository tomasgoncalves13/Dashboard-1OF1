import { ModulePlaceholder } from "@/components/module-placeholder";
export default function ExpensesPage() {
  return <ModulePlaceholder title="Expenses" description="Recurring & one-off costs by category, with invoice uploads." phase="Phase 6 — Expenses" />;
}
