import { ModulePlaceholder } from "@/components/module-placeholder";

export default function InventoryPage() {
  return (
    <ModulePlaceholder
      title="Inventory"
      description="Live stock per SKU with movement log and reorder alerts."
      phase="Phase 3 — Stock engine"
    />
  );
}
